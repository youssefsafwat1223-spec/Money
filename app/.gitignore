# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
# ملاحظة: pubspec.lock يُلتزَم به (committed) في التطبيقات — لا تتجاهله.

# Generated code (drift/freezed/json) — يُولَّد بـ build_runner
*.g.dart
*.freezed.dart

# IDE
.idea/
.vscode/
*.iml

# Platform build artifacts (تُولَّد بـ flutter create)
android/.gradle/
android/local.properties
ios/Pods/
ios/.symlinks/
.DS_Store

# Secrets / local env
*.env
key.properties
**/google-services.json
**/GoogleService-Info.plist
