# Phase 0.5 Repo Preparation Self Review

Date: 2026-06-24

## Result

FAIL.

The dirty tree was successfully backed up and stashed, and only approved docs/images were restored. However, validation did not pass and the macOS build generated risky dirty files. Phase 1 Tokens cannot start.

## Files Created

- `docs/redesign/phase_0_5_repo_preparation_report.md`
- `docs/redesign/phase_0_5_repo_preparation_self_review.md`

## Safety Confirmations

- No Flutter source files were restored from the stash.
- No business logic was restored from the stash.
- No providers were restored from the stash.
- No routes were restored from the stash.
- No `app/pubspec.yaml` or `app/pubspec.lock` files were restored from the stash.
- No `app/macos/**` files were restored from the stash.
- No helper patch/rewrite scripts were restored.
- No stash was dropped.
- No UI implementation was started.
- No Phase 1 work was started.
- No files were staged.
- No commit was made.
- `git add .` was not used.

## Backup Confirmations

Created:

```text
../MALI_PRE_PHASE1_tracked_dirty_backup.patch
../MALI_PRE_PHASE1_staged_backup.patch
../MALI_PRE_PHASE1_status_backup.txt
```

Stash created:

```text
stash@{0}: On feat/accounts-multicurrency: backup: dirty tree before Mali reference Phase 1 tokens
```

Existing older stash preserved:

```text
stash@{1}: On feat/accounts-multicurrency: wip: isolate non-phase1 changes before token foundation
```

## Restored Files

Restored only:

```text
docs/design_reference/mali_reference_analysis.md
docs/design_reference/mali_visual_adaptation_spec.md
docs/design_reference/mali_screen_migration_plan.md
docs/design_reference/mali_codex_execution_guardrails.md
docs/redesign/phase_0_reference_baseline_report.md
docs/redesign/phase_0_reference_self_review.md
docs/claude_design_briefs/00_onboarding_current_inventory.md
docs/claude_design_briefs/01_onboarding_claude_design_brief.md
docs/claude_design_briefs/01_onboarding_claude_design_prompt.md
app/docs/images/
```

## Not Restored

Confirmed not restored:

```text
app/lib/**
app/pubspec.yaml
app/pubspec.lock
app/macos/**
supabase/**
patch/rewrite helper scripts
PRE_REVERT_uncommitted_tweaks.patch
docs/claude_design_briefs/.01_onboarding_claude_design_prompt.md.swp
tmp/pdfs/**
app/mali_ui_explorer.html
mockups/mali_html_redesign/index.html
```

## Validation Results

`flutter analyze`: FAIL.

Reason:

- 19 analyzer issues in existing Flutter UI files on clean HEAD.
- Representative failures: missing `AppLucideIcons.pieChart`, `penLine`, `sparkles`, `tag`, `calendarDays`; invalid `TransactionTypeEntity.expense`; missing `CategoryView.iconData`, `CategoryView.colorValue`, `TransactionEntity.confidence`; invalid `categoryId` parameter.

`flutter test`: PASS.

Result:

```text
01:09 +241: All tests passed!
```

`flutter build macos --debug`: FAIL.

Reason:

- Sandboxed attempt failed on Flutter SDK cache permissions.
- Escalated attempt reached compilation and failed on the same clean-HEAD UI errors as analyzer.

## Remaining Risks

Validation generated these risky files:

```text
 M app/macos/Runner.xcodeproj/project.pbxproj
 M app/macos/Runner.xcworkspace/contents.xcworkspacedata
?? app/macos/Podfile.lock
```

They were not restored from the stash and were not intentionally edited. They appeared after running the required macOS build gate. Per instruction, they were not deleted, reverted, stashed, or cleaned.

## Phase 1 Readiness

Phase 1 Tokens cannot start.

Required before Phase 1:

- Resolve or intentionally baseline clean-HEAD analyzer/build failures in a separate repair step.
- Decide what to do with the macOS files generated by validation.
- Rerun validation gates.
- Commit safe reference baseline only after gates pass and remaining dirty files are approved docs/images/reports only.
